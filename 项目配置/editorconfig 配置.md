```properties
[*.{js,jsx,mjs,cjs,ts,tsx,mts,cts,vue}] # 适用于这些文件类型
charset = utf-8 # 设置文件编码为 UTF-8
indent_size = 2 # 缩进大小为 2 个空格
indent_style = space # 使用空格进行缩进
insert_final_newline = true # 在文件末尾插入一个新行
trim_trailing_whitespace = true # 自动去除行尾多余的空白
end_of_line = lf # 设置行尾符号为 LF（换行）
```

